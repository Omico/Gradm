rootProject.name = "gradm-with-kotlin-multiplatform-gradm"

pluginManagement {
    repositories {
        mavenLocal()
        maven(url = "https://s01.oss.sonatype.org/content/repositories/snapshots")
        mavenCentral()
        gradlePluginPortal()
    }
}
